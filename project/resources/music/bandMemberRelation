SELECT
		(REPLACE(str(?memberOf), "http://www.wikidata.org/entity/", "") as ?authorId)
		(REPLACE(str(?band), "http://www.wikidata.org/entity/", "") as ?artWorkId)
		(?memberOfLabel as ?authorName)
		(?bandLabel as ?artWorkTitle)
 WHERE
{
		?band			wdt:P31			wd:Q215380 .
		?band			rdfs:label		?bandLabel .
  		?band			wdt:P527		?memberOf .
		?memberOf		rdfs:label		?memberOfLabel .

  		FILTER (lang(?memberOfLabel) = 'en') .
  		FILTER (lang(?bandLabel) = 'en') .
}
GROUP BY ?memberOf ?band ?memberOfLabel ?bandLabel
ORDER BY ?memberOfLabel